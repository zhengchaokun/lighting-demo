

/*App.registView("start",new App.View({
  el:"#start",
  model:new App.Model()
}),true);*/

App.registView("list",new App.View({
  el:"#list",
  model:new App.Model()
}),true);

App.registView("detail",new App.View({
  el:"#detail",
  model:new App.Model()
}),false);

      